# Trading data preparation dependencies
python-binance>=1.0.19  # Binance API for crypto data
pandas>=2.0.0
numpy>=1.24.0
ta>=0.10.2
scikit-learn>=1.3.0
matplotlib>=3.7.0  # For visualization (optional)
pyarrow>=14.0.0    # For efficient data storage

# MLX for Apple Silicon (if we keep MLX for some computations)
mlx>=0.19.0